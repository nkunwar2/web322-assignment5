<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/css/main.css">
  <title>Site Collection - National Historic Sites</title>
</head>
<body>
  <%- include('partials/navbar', {page: page }) %>

  <div class="container mx-auto px-4 py-8">
    <div class="hero bg-base-200 mb-8 p-4 rounded-lg">
      <div class="hero-content text-center">
        <div>
          <h1 class="text-4xl font-bold">Site Collection <span class="text-base text-primary">(<%= sites.length %>)</span></h1>
          <p class="py-2">Browse through Canada's National Historic Sites</p>
        </div>
      </div>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
      <% sites.forEach(site => { %>
        <div class="card bg-base-100 shadow-xl">
          <figure>
            <img src="<%= site.image %>" alt="<%= site.site %>" class="h-48 w-full object-cover" onerror="this.src='https://placehold.co/600x400?text=No+Image'">
          </figure>
          <div class="card-body">
            <h2 class="card-title"><%= site.site %></h2>
            <p class="text-sm text-gray-500"><%= site.ProvinceOrTerritory.name %>, <%= site.ProvinceOrTerritory.region %></p>
            <p class="line-clamp-3"><%= site.description %></p>
            <div class="card-actions justify-end mt-4">
              <a href="/sites/<%= site.siteId %>" class="btn btn-primary">View Details</a>
            </div>
          </div>
        </div>
      <% }) %>
    </div>
  </div>
</body>
</html>
